<div class="col-lg-4">
    <div class="row" style="text-align:center;margin-top:125px;padding:10px;">
        {% if invitationAlreadySend == true %}
            <div>
                <div class="alert alert-info"><strong>Information :</strong> Vous avez déjà envoyé une invitation.</div>
            </div>
        {% elseif invitationIsSendToMe == true %}
            <div>
                <div class="alert alert-info">
                    <strong>Information :</strong> Cette personne vous à envoyé une invitation.<br />
                    Vous pouvez choisir d'accepter cette invitation ou la refuser :<br />
                    <span id="btNewInvitation{{ userInformation.id }}">
                        <a onclick="acceptInvitation({{ userInformation.id }});" id="btAcceptInvit{{ userInformation.id }}" class="btn btn-success btn-xs clickable acceptInvit">Accepter <i class="fa fa-check" aria-hidden="true"></i></a>
                        <a onclick="refuseInvitation({{ userInformation.id }});" id="btRefuseInvit{{ userInformation.id }}" class="btn btn-danger btn-xs clickable refuseInvit">Refuser <i class="fa fa-times" aria-hidden="true"></i></a>
                    </span>
                </div>
            </div>
        {% else %}
            <div class="alert alert-info" id="loadSendInvit" style="display:none;">
                <i class="fa fa-spinner fa-pulse fa-fw"></i> Envoi de l'invitation en cours.
            </div>
            <div class="alert alert-success" id="successSendInvit" style="display:none;">
                <i class="fa fa-check" aria-hidden="true"></i> <strong>Information :</strong> L'invitation à été envoyé avec succés.
            </div>
            <div class="btn-group btn-group-justified">
                <a onclick="sendInvitation({{ userInformation.id }});" id="btSendInvitation" class="btn btn-primary">
                    Envoyer une invitation
                </a>
            </div>
        {% endif %}
    </div>
</div>
<div class="col-lg-8">
    <div class="row" style="margin-top:30px;">
        <div class="alert alert-warning"><strong>Information :</strong> Vous ne pouvez voir les actualités de ce profil car vous n'êtes pas amis avec cette personne.</div>
    </div>
</div>
