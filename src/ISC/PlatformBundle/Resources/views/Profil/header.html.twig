{% if app.user.username  == app.request.get('username') %}
    {{ include("ISCPlatformBundle:Profil/Header:myHeader.html.twig") }}
{% else %}
    {{ include("ISCPlatformBundle:Profil/Header:userHeader.html.twig") }}
{% endif %}
